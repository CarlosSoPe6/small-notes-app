import React, { FC } from 'react';

const ServerError: FC<{}> = function ServerError(): JSX.Element {
  return (
    <div className="home-view">
        Home view
    </div>
  );
};

export default ServerError;

import React, { FC } from 'react';

const ClientError: FC<{}> = function ClientError(): JSX.Element {
  return (
    <div className="home-view">
        Home view
    </div>
  );
};

export default ClientError;
